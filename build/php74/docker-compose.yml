version: "3.5"
services:
  centos7igbinaryphp74build:
    networks:
      - buildnet
    build:
      network: buildnet
      context: .
      args:
        image: php-7.4
    image: aursu/peclbuild:7-php-igbinary-7.4
  centos8igbinaryphp74build:
    networks:
      - buildnet
    build:
      network: buildnet
      context: .
      args:
        centos: 8
        image: php-7.4
    image: aursu/peclbuild:8-php-igbinary-7.4

networks:
  buildnet:
    external: true
    name: buildnet
