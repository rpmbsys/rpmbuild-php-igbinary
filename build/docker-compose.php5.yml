version: "3.5"
services:
  centos6igbinaryphp5build:
    networks:
      - buildnet
    build:
      context: .
      args:
        centos: 6
        image: php-5.6
      network: buildnet
    image: aursu/peclbuild:6-php-igbinary-5.6
  centos7igbinaryphp5build:
    networks:
      - buildnet
    build:
      context: .
      args:
        image: php-5.6
      network: buildnet
    image: aursu/peclbuild:7-php-igbinary-5.6

networks:
  buildnet:
    external: true
    name: buildnet
